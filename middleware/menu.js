export default function ({ store, route, redirect, $axios, app }) {
  // window.console.log(
  //   'middleware menu.js',
  //   '从store/menu.js 中获取的menu data'
  // )
  // store.dispatch('menu/GetMenuInfo', null)
}
